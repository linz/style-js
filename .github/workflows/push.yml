name: Build

on:
  merge_group:
  pull_request:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@064fe7f3312418007dea2b49a19844a9ee378f49 # v6.0.2
        with:
          fetch-depth: 0

      - uses: linz/action-setup-node@9bbffa85881932a5a08a231bcadcdcae1144518e # v1.0.6
        with:
          node-version-file: .node-version
          registry-url: 'https://registry.npmjs.org'
          scope: '@linzjs'

      - run: npm ci
      - run: npm run build
      - run: npm run lint
      - run: npm run test
